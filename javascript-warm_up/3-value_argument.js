#!/usr/bin/node
//  a script that prints the first argument passed to it:
function printArgs(arg1, arg2) {
  console.log(arg1 + 'is' + arg2);
}
// calling the arguments with sample arguments
printArgs('Javascript', 'amazing');
